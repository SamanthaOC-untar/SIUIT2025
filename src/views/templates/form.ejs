<form method="POST" action="/tambah"> <!-- Submit ke route Express -->
  <div class="form-group">
    <label>Sumber</label>
    <input type="text" name="sumber" value="<%= data.sumber || '' %>" required>
  </div>
  <!-- ... field lainnya ... -->
  <button type="submit">Simpan</button>
</form>